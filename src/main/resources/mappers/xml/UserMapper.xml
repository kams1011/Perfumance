<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Kams.Perfumance.mapper.UserMapper">
    <select id="getList" resultType="User_Info">
        SELECT
          *
        FROM user_info
    </select>


    <insert id="Insert">
        insert into user_info value()
    </insert>



</mapper>